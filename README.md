# 📊 CSV Data Viewer

A full‑stack application for uploading, storing, and searching CSV data.  
Built with **React** (frontend), **Node.js/Express** (backend), and **MongoDB** (database).  
Everything runs inside Docker using **Docker Compose**.

---

## 🚀 Getting Started

### Prerequisites
- [Docker](https://docs.docker.com/get-docker/) installed
- [Docker Compose](https://docs.docker.com/compose/install/) installed

No need to install Node.js or MongoDB locally — everything runs in containers.

---

## ▶️ Running the Project

1. **Unzip the project**
   unzip st-assignment.zip
   cd st-assignment

2. **Start containers**
   docker-compose up --build

3. **Access the app**
   Frontend: http://localhost:3000
   Backend API: http://localhost:5000
